ADMINER_VIRTUAL_HOST=dt-adminer.laravel.test
ADMINER_HOST_HTTP_PORT=9003

API_VIRTUAL_HOST=api.laravel.test
API_HOST_HTTP_PORT=8001

BEANSTALKD_CONSOLE_VIRTUAL_HOST=dt-queue.laravel.test
BEANSTALKD_CONSOLE_HOST_HTTP_PORT=9004

FRONT_VIRTUAL_HOST=front.laravel.test
FRONT_HOST_HTTP_PORT=8002

GENERAL_TIMEZONE=UTC

LARAVEL_ECHO_SERVER_VIRTUAL_HOST=broadcasting.laravel.test
LARAVEL_ECHO_SERVER_HOST_HTTP_PORT=8003

MAILCATCHER_CLIENT_VIRTUAL_HOST=dt-mailcatcher.laravel.test
MAILCATCHER_CLIENT_HOST_HTTP_PORT=9002

MARIADB_DATABASE=development
MARIADB_USER=sa
MARIADB_PASSWORD=StrongP@ssw0rd
MARIADB_HOST_PORT=3306
MARIADB_ROOT_PASSWORD=StrongP@ssw0rd
MARIADB_ENTRYPOINT_INITDB=./docker/services/mariadb/docker-entrypoint-initdb.d

MSSQL_HOST_PORT=1433
MSSQL_PID=Developer
MSSQL_DATABASE=development
MSSQL_SA_PASSWORD=StrongP@ssw0rd

NGINX_VERSION=1.13.12

PATH_API_SRC=./src/api
PATH_FRONT_SRC=./src/front

PGADMIN_DEFAULT_USER=sa@laravel.test
PGADMIN_DEFAULT_PASSWORD=StrongP@ssw0rd

PHP_FPM_VERSION=7.2
PHP_FPM_INSTALL_INTL=false
PHP_FPM_INSTALL_IMAGEMAGICK=false
PHP_FPM_INSTALL_IMAGE_OPTIMIZERS=false

PHPMYADMIN_USER=sa
PHPMYADMIN_PASSWORD=StrongP@ssw0rd
PHPMYADMIN_ROOT_PASSWORD=StrongP@ssw0rd
PHPMYADMIN_VIRTUAL_HOST=dt-phpmyadmin.laravel.test
PHPMYADMIN_HOST_HTTP_PORT=9005

POSTGRES_DB=development
POSTGRES_USER=sa
POSTGRES_PASSWORD=StrongP@ssw0rd
POSTGRES_HOST_PORT=5432

PGADMIN_VIRTUAL_HOST=dt-pgadmin.laravel.test
PGADMIN_HOST_HTTP_PORT=9006

PROXY_HOST_HTTP_PORT=80

REDIS_HOST_PORT=6379

REDIS_COMMANDER_VIRTUAL_HOST=dt-redis.laravel.test
REDIS_COMMANDER_HOST_HTTP_PORT=9001
